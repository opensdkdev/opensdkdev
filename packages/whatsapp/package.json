{
  "name": "@opendevsdk/whatsapp",
  "private": true,
  "publishConfig": {
    "access": "restricted"
  },
  "scripts": {
    "build": "pnpm provision && pnpm definition && pnpm generate",
    "generate": "fern generate --group typescript --force",
    "definition": "fern write-definition",
    "check": "fern check"
  },
  "packageManager": "pnpm@10.31.0",
  "devDependencies": {
    "fern-api": "4.26.0"
  }
}
